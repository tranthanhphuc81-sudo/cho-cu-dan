# Server Configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/cho-cu-dan

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Admin seed defaults (used by scripts/seedAdmin.js)
# Set these before running `npm run seed:admin` if you want different credentials
ADMIN_EMAIL=admin@local.test
ADMIN_PASSWORD=admin123
